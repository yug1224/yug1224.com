extends lib/layout.jade

block head
  meta(name="robots", content="noindex")

block content
  article
    header.page-header
      h1.entry-title!= "Apps"

  each app in apps
    article
      h3.entry-title
        a(href=app.url)!= app.title
